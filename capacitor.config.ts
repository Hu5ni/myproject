import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nyp.RedoPractical',
  appName: 'RedoPractical',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
